#include "Warehouse.h"

void Warehouse::addItem(Item i) {

    for (int i = 0; i < count; i++)
    {
        for (int j = 0; j < count; j++)
        {
            for (int k = 0; k < count; k++)
            {
                seek(i, j, k)
            }
            
        }
        
    }
    
}
void seek(Item addedItem, Shelf* s, Rack* r, Item* m, int& slider1, int& slider2, int& slider3) {

    for (int i = 0; i < count; i++)
    {
        for (int j = 0; j < count; j++)
        {
            for (int k = 0; k < count; k++)
            {
                r[i].shelf[i].item[i]
            }
            
        }
        
    }
    
}